import { Wrapper } from "@/components/fleet/buy/wrapper";                   

export default async function Buy() {
    return (

        <main className="flex w-screen h-screen bg-[url('/images/dodo.svg')] bg-center bg-cover">
                <Wrapper/>
        </main>
    )
}